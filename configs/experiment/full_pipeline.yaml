# Experiment: Full 5-stage pipeline
# Usage: python scripts/train.py +experiment=full_pipeline
defaults:
  - override /data: simulation
  - override /training: default

pipeline:
  stages: [A, B, C, D, E]

training:
  epochs: 200
