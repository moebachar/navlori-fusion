# AI-Assistance Usage Log

This file records every AI-assisted passage in the ICINCO 2026 paper so the
camera-ready Acknowledgements disclosure can be written from it (ICINCO
requires disclosing AI-generated text and citing the AI system). Identifying
details stay out of the review draft; the disclosure text is finalized for the
camera-ready only.

| Date | Section | What was AI-assisted | Human review status |
|---|---|---|---|
| 2026-06-01 | 3. Methodology (3.1-3.5) | Full first draft of the Methodology section (problem formulation, pipeline overview with the token / continuous-time / attention / readout equations, per-modality encoder descriptions, training-and-async-robustness paragraph) drafted with AI assistance. Every quantitative and architectural claim was grounded in repository source files (transformer.py, wifi.py, imu.py, fusion_trainer.py, builder.py, configs/stage_c/fusion.yaml, configs/data/simulation_2mod.yaml) and annotated with `%` source comments; parameter counts were measured directly with PyTorch, not invented. | Pending author review |
| 2026-06-01 | Title block + figure-1 (main.tex, draft.tex, sections/03_methodology.tex) | Compliance/format-only review-and-correct pass over a non-paper-writer's edits. No new prose or results generated. AI actions: (1) added LaTeX `%` comments documenting the ORCID iDs and the missing official `orcid.eps` asset (no icon fabricated, no `\orcidAuthor` calls added); (2) added a `%` note recording the `\blindfalse` single-blind deviation vs the contract's double-blind default and preserving the `\ifblind` toggle; (3) added a `%` scope-warning block flagging `figures/figure_1.jpg` as the out-of-scope decomposed dual-head diagram contradicting section 3.4. All changes are comments/annotations only; no author identities, names, or numbers were invented. | Pending author/director review |
| 2026-06-01 | Title block (main.tex, draft.tex) | Format-only pass applying four director decisions; no new prose or results. AI actions: (1) full author names RESOLVED by director and applied -- "I.~Abouelaziz" -> "Ilyass Abouelaziz", "Y.~Ding" -> "Yuehua Ding" (author 1 "Mohamed Bachar" unchanged); director-supplied, NOT invented; (2) ORCIDs WIRED with `\orcidAuthor{<id>}` immediately after each author name per the SCITEPRESS template (Example.tex pattern), using the OFFICIAL `orcid.eps` copied from the SCITEPRESS author kit into paper/ (no icon fabricated); (3) `\blindfalse` updated to read as director-CONFIRMED single-blind (was a tentative deviation), `\ifblind` toggle preserved; (4) Figure 1 left untouched -- director owns the in-scope single-PositionQuery diagram. Stale "pending first names" / "missing orcid.eps" comment lines removed since both are now resolved. | Pending author/director review |
| 2026-06-01 | 3. Methodology (draft.tex ONLY) | Director-validated structural restructure of the Methodology bullet outline; no new results, no numbers invented. AI actions, all in `paper/draft.tex` (main.tex / sections/* untouched): (1) swapped the order of the first two subsections so it reads Overview -> Problem Formulation -> [unchanged steps]; (2) renamed "Pipeline Overview" -> "Overview" and enriched it into a global, low-symbol map (thesis line, end-to-end Fig.~1 path, universal-token CONCEPT only, why-this-shape, robustness preview, roadmap); (3) reduced "Problem Formulation" to pure formalism (asynchronous multi-rate streams, $K$ instants, availability flag $a_{m,k}$, per-observation triple, set-not-tensor, target at $t_q$/$\tau$, missing-modality and staleness stated as formal conditions, notation) -- removed intro-like motivational framing; (4) relocated the continuous-time math OUT of Overview into Per-Modality Encoders: the universal-token equation bullet (carrying `% src: transformer.py:218-224`) and the two $\phi$ continuous-time-encoding bullets (carrying `% src: transformer.py:53-66`), leaving only a forward-pointing one-line mention in Overview. The Per-Modality Encoders / WiFi-Net / IMUCNN, Set-Transformer Fusion and Position Readout, and Training and Async-Robustness subsections are otherwise intact. Internal `\S3.x` cross-references re-pointed to the new numbering (encoders 3.3, fusion/readout 3.4, training 3.5). Could NOT compile -- no LaTeX/detex engine on this machine. | Pending author/director review |
| 2026-06-01 | 3. Methodology — §3.1 Overview + §3.2 Problem Formulation ONLY (paper/sections/03_methodology.tex) | Revision pass on existing prose applying two director takes: (1) register lift to a scientific/SCITEPRESS level — replaced plain/explanatory phrasings (e.g. "fuses ... into a planar position estimate" -> "regresses planar position $(x,y)$ from"; "records what was sensed / which sensor / when" -> "jointly encodes the measured signal / the originating sensor / the acquisition time"; "makes ... the same operation" -> "collapses ... into a single operation"; "nothing in the trunk changes with the count" -> "invariant to token count ... through the same trunk"; "treats the token as a concept" -> "As a concept, a universal token jointly encodes ..."; "carries two sensors that report at incompatible rates" -> "is instrumented with two sensors of incompatible acquisition rate"; "names the two properties" -> "admits two degradation conditions") with all technical meaning and claims preserved; (2) shortened the Overview from ~5 paragraphs to 2 compact paragraphs by merging and cutting redundancy while keeping every concept (thesis, end-to-end Fig.~1 path, universal-token CONCEPT-only with forward pointer to §3.3, one-block rationale = cross-modal==cross-time + permutation-invariance, robustness preview pointing to §3.5, roadmap). Overview remains CONCEPT-ONLY (no token equation, no $\phi$ math). Problem Formulation remains pure formalism (no motivational framing re-added). All `% src:` traceability comments kept attached to their claims; Figure 1 float, caption, scope-warning comment block, and [[VERIFY: figure-1 asset]] marker left EXACTLY as-is. No new numbers/claims. §3.3/§3.4/§3.5, main.tex, draft.tex untouched. Could NOT compile — no LaTeX/detex engine on this machine; verified by reading that `\ref{fig:pipeline}` and the figure `\label` still resolve and the figure block is intact. | Pending author/director review |
| 2026-06-01 | 3. Methodology — §3.2 Problem Formulation ONLY (paper/sections/03_methodology.tex) | Director-take revision: recast the §3.2 OPENING to a pure formal problem statement, removing the intro-like scene-setting. AI actions: replaced the narrative opening ("A mobile agent is instrumented with two sensors of incompatible acquisition rate: a WiFi radio emitting ... an IMU streaming ...") with a "Let $\mathcal{M}=\{$wifi,imu$\}$ ... Given a window of $K$ instants ... The task is to estimate ..." formalism. Asynchrony RETAINED but reframed as a formal property of the setup (modalities sampled at incompatible nominal rates, wifi ~1 Hz / imu ~30 Hz => streams unaligned and of unequal rate => no observation assumed for every modality at every instant) rather than as a descriptive sentence about an agent and its sensors. All formal objects/values unchanged in meaning ($K$, the triple $(s,a,\Delta t)$, $a\in\{0,1\}$, $\Delta t=t_{obs}-t_q\le0$, missing-modality $a_{m,k}=0\,\forall k$, staleness large $|\Delta t|$, $\mathcal{M}=\{$wifi,imu$\}$, $M=2$, $D=128$). All four `% src:` comments preserved and re-attached to their claims (simulation_2mod.yaml:9 modalities; :21-24 rates; transformer.py:27-32 triple/dt; fusion.yaml:26 embed_dim 128). No new numbers/claims. §3.1 Overview, the Figure 1 block, §3.3/§3.4/§3.5, main.tex, draft.tex untouched; both `Section~3.2` cross-refs still resolve. Could NOT compile — no LaTeX engine on this machine; verified by reading. | Pending author/director review |
| 2026-06-01 | 3. Methodology — §3.3 Per-Modality Encoders (token lead-in, WiFi-Net, IMUCNN) + §3.4 Set-Transformer Fusion and Position Readout + §3.5 Training and Async-Robustness ONLY (paper/sections/03_methodology.tex) | Style-only register lift of §3.3-§3.5 to match the scientific/SCITEPRESS register already set in §3.1 Overview and §3.2 Problem Formulation. No structural change, no reordering, no new content, no numbers invented or altered. AI actions: replaced plain/explanatory phrasings with crisp formal equivalents while preserving every claim's meaning -- e.g. §3.3 "Two encoders turn raw samples into ... so both drop into the fusion block without per-modality wiring" -> "Two encoders map raw samples to ... Each projects its own input shape onto the common width, so both enter the fusion block without per-modality wiring"; "what lets one model absorb sensors at 1 Hz and 30 Hz" -> "admits sensors at 1 Hz and 30 Hz into one model"; "Anchor pooling suits WiFi's role ... turns it into a fingerprint" -> "Anchor pooling matches WiFi's role ... casts it as a fingerprint"; §3.4 "Read-out uses a learnable PositionQuery" -> "Read-out is performed by a learnable PositionQuery", "Keeping the CLS token ... carries the same not-a-number safety" -> "Retaining the CLS token ... extends the same not-a-number safety"; §3.5 "comes from training" -> "derives from training", "teaches the model to operate ... leaning on the freshest one" -> "conditions the model to operate ... rather than the freshest one ... subsumes both missing modalities and staleness". All equations and labels (eq:token, eq:time, eq:attn, eq:readout) unchanged in math and `\label`; all `% src:` traceability comments kept attached to their claims; the `% TODO` K-reconciliation marker and the §3.5 `\textcolor{red}` note preserved. Every quantitative/architectural value unchanged (k=64, ~0.05 M params, 9-feature 32-step window, 32->64->128 channels, 6 layers, 4 heads, 4D FFN, n=32 sinusoids, [0.05,120] s, 90 epochs, lr 1.3e-3, wd 1e-4, Huber delta 0.5, grad-clip 1.0, B=128, K=4, seed 42, modality_dropout 0.4, instant_dropout 0.45). Scope held: single PositionQuery readout only; no decomposed variant introduced. §3.1, §3.2, the Figure 1 float/caption/scope-warning comment, main.tex, draft.tex untouched. Could NOT compile -- no LaTeX/detex engine on this machine; verified by reading that all `\label`/`\ref` resolve and equations/src comments are intact. | Pending author/director review |
| 2026-06-01 | 3. Methodology — inter-subsection continuity (§3.3 Per-Modality Encoders, WiFi-Net, IMUCNN, §3.4 Set-Transformer Fusion and Position Readout, §3.5 Training and Async-Robustness) ONLY (paper/sections/03_methodology.tex) | Flow-only continuity pass making the Methodology read as one narrative arc; no content, structure, equations, or numbers changed. AI actions: rewrote the OPENING sentence of each step subsection to bridge from the prior step's output instead of opening cold/standalone, with varied (non-templated) connectives — §3.3 now opens "The set of available observations from Section 3.2 enters the model through two encoders ..."; WiFi-Net opens "The two encoders differ because their inputs play opposite roles in the fusion. The first, WiFi-Net, reads the modality that fixes absolute place ..."; IMUCNN opens "Where WiFi-Net reads one sparse scan as a place fingerprint, the second encoder reads dense motion over a temporal window. IMUCNN encodes ..." (the two encoder subsubsections now read as a contrasting pair rather than two parallel "<Name> encodes ..." openings); §3.4 opens "The two encoders emit $K\cdot M$ tokens of equal width, one per available observation, and these enter a single self-attention block ..." (links to the token SET the encoders produce); §3.5 opens "The trunk just described carries no branch for a missing or stale sensor; the tolerance previewed in Section 3.1 is instilled instead by how the model is trained. Models are trained for 90 epochs ...". §3.2 Problem Formulation OPENING preserved EXACTLY ("Let $\mathcal{M}=...$"), per guardrail — its continuity comes from the §3.1 roadmap, which was left intact. All equations and labels (eq:token, eq:time, eq:attn, eq:readout) unchanged; all 29 `% src:` comments preserved; the `% TODO` K-reconciliation marker and the §3.5 `\textcolor{red}` note and the figure-caption `\textcolor{red}` note preserved; Figure 1 float/caption/scope-warning comment untouched. Every quantitative/architectural value unchanged (k=64, ~0.05 M params, 9-feature 32-step window, 32->64->128 channels, 6 layers, 4 heads, 4D FFN, n=32 sinusoids, [0.05,120] s, single PositionQuery readout, 90 epochs, lr 1.3e-3, wd 1e-4, OneCycleLR, Huber delta 0.5, grad-clip 1.0, B=128, K=4, seed 42, modality_dropout 0.4, instant_dropout 0.45, rescue pass). Scope held: single PositionQuery only; no decomposed variant. main.tex, draft.tex, Group-1 template files untouched. Could NOT compile — no LaTeX/detex engine on this machine; verified by reading that all labels/refs resolve and src comments/markers/numbers are intact. | Pending author/director review |
| 2026-06-01 | 3. Methodology (paper/sections/03_methodology.tex) + Experiments setup (paper/main.tex, Exp-setup subsection ONLY) | Director-approved rigor + symbolization pass; no results changed, no numeric value altered in meaning. TAKE 1 (foundational rigor, §3.4 of 03_methodology.tex): (a) added Algorithm~1 `\label{alg:forward}` via algorithm2e — the end-to-end forward pass (assemble one universal token per available observation per Eq.~token, prepend never-masked CLS, run L pre-norm self-attention layers over [CLS \|\| K*M tokens] with the boolean padding mask, single PositionQuery cross-attends the contextualized set with optional $\phi(\tau)$ shift, 2-layer MLP -> (x,y)); faithful to transformer.py:200-227/256-288, single-PositionQuery only (no decomposed variant). (b) New equations formalizing prose-only mechanisms: `eq:wifi_assign` (WiFi-Net soft assignment $\mathbf{w}=\mathrm{softmax}(\mathbf{A}s/g)$) and `eq:wifi_token` (token = residual-MLP over $\sum_j w_j \mathbf{E}_j$), src wifi.py:43-72/86-97; `eq:imu` ($\mathrm{LN}(\mathbf{W}\,\mathrm{GAP}(\mathrm{ConvStack}(\mathbf{X})))$), src imu.py:55-78; the single attention eq (was `eq:attn`) expanded into `eq:layer` (two pre-norm residual sub-layers MHSA+FFN) and `eq:mhsa` (per-head softmax + concat through $\mathbf{W}_O$, $d_h=D/H$), src fusion.yaml:24-30 + transformer.py:131-138. No Algorithm 2, no dropout-mask eqs, no permutation Remark (out of scope). TAKE 2 (symbolization): defined a collision-free symbol set used in §3.3-§3.5 prose instead of inline numbers — $D$=token width, $M$=modalities, $N_a$=WiFi anchors (RENAMED from lowercase $k$ to disambiguate from $K$=instants, every occurrence updated), $T$=IMU window, $F$=IMU features, $(C_1,C_2,C_3)$=conv channels, $L$=self-attention layers (chosen to avoid clashing with lengths), $H$=heads, $r$=FFN ratio (width $rD$), $n$=sinusoids with period range $[\tau_{\min},\tau_{\max}]$; added architecture config Table~`\label{tab:arch}` at end of §3.4 listing every symbol+value (D=128,M=2,N_a=64,T=32,F=9,channels (32,64,128),L=6,H=4,r=4,n=32,periods [0.05,120] s,~0.05 M params/encoder), prose now cites the table for values. Moved the training-hyperparameter dump out of §3.5 into a training-config Table~`\label{tab:train}` placed in main.tex INSIDE `\subsection{Exp setup}` (the only main.tex edit; everything else in main.tex untouched); §3.5 now reads symbolically (AdamW/OneCycleLR/Huber/grad-clip/seed described methodologically; dropout mechanism kept with rates $p_m$, $p_i$ and the rescue pass) and references Table~tab:train. Removed the §3.5 `\textcolor{red}{this paragraph should be in the subsection exp setup}` marker (the move fulfills it); ALL other red notes preserved (§3.1 overview note, Figure 1 caption note, main.tex title-block notes). Every numeric value unchanged in meaning (only presentation converted from inline number to symbol+table); all `% src:` comments preserved and carried with relocated content (36 in 03_methodology.tex + the relocated training-config src block in main.tex); all `\label`/`\ref` resolve (incl. new alg:/tab:/eq: refs); Figure 1 float/caption/scope-warning/[[VERIFY]] untouched. Could NOT compile — no LaTeX/detex engine on this machine; verified by reading. | Pending author/director review |
| 2026-06-01 | 3. Methodology (paper/sections/03_methodology.tex — the MAIN paper file main.tex \input's) | Brought the main-paper Methodology into the director-validated structure from draft.tex, as full publication PROSE (not bullets); no new results, no numbers invented. AI actions: (1) REORDER + RENAME -- swapped the first two subsections so it reads Overview -> Problem Formulation, renaming "Pipeline Overview" -> "Overview"; the step subsections (Per-Modality Encoders [WiFi-Net, IMUCNN] -> Set-Transformer Fusion and Position Readout -> Training and Async-Robustness) keep their order. (2) Rewrote the Overview as a global, concept-only map (thesis: one continuous-time set-transformer fuses async WiFi+IMU into (x,y), no per-modality branches/no resampling; end-to-end Fig.~1 path; universal token described as concept = what/which/when with a forward pointer to Section 3.3 for the equation + $\phi$ math; why-this-shape = one token space + one attention block makes cross-modal == cross-time fusion and permutation-invariance; one-sentence robustness preview pointing to Section 3.5; roadmap). The Overview is CONCEPT-ONLY -- it contains NO token equation and NO $\phi$ sinusoid math. (3) Rewrote Problem Formulation as pure formalism -- CUT the motivational/intro framing (e.g. "The goal is to predict ..." narrative lead, the "This formulation names ..." intro phrasing reworked to formal condition statements); KEPT async multi-rate streams (WiFi ~1 Hz / IMU ~30 Hz, unaligned/unequal-rate), $K$ instants, per-observation triple $(s,a,\Delta t)$ with $a\in\{0,1\}$ and $\Delta t=t_{obs}-t_q\le 0$, set-not-tensor, target $(x,y)$ at $t_q$ (optionally offset $\tau$), missing-modality ($a_{m,k}=0\,\forall k$) and staleness (large $|\Delta t|$) stated formally, and notation ($\mathcal{M}=\{$wifi,imu$\}$, $M=2$, $K$, $D=128$). (4) RELOCATED the token equation (`eq:token`) and continuous-time encoding equation (`eq:time`) plus their explanatory prose OUT of Overview and INTO Per-Modality Encoders as the token-construction lead-in before WiFi-Net; labels eq:token / eq:time preserved; their `% src:` comments carried with them. Equation document order is now eq:token, eq:time (Encoders) then eq:attn, eq:readout (Fusion/Readout). (5) Updated the one stale hardcoded section number in the Training subsection ("two data properties of Section~3.1" -> "Section~3.2") so it still points at Problem Formulation under the new order. The Set-Transformer Fusion and Position Readout and Training and Async-Robustness prose are otherwise unchanged. Figure 1 float, caption, the scope-warning comment block, and the [[VERIFY: figure-1 asset]] marker were left EXACTLY as-is. All `\label`/`\ref` verified by reading (only cross-ref into the section is `\ref{fig:pipeline}`, which resolves; no equation is `\ref`'d by number). Could NOT compile -- no LaTeX/detex engine on this machine. | Pending author/director review |
| 2026-06-01 | 4. Experiments — Experimental Setup subsection ONLY (paper/main.tex) + one-line baselines edit (paper/draft.tex) | First publication PROSE for the Experiments section's Experimental Setup subsection, drafted with AI assistance; no results numbers invented. AI actions in paper/main.tex (only inside \subsection{Exp setup}, renamed to \subsection{Experimental Setup}): (1) wrote the datasets prose — Webots simulation (controlled lab / ablations, WiFi+IMU, split train [1,3-12] / val [2,13,14] / test [15,16,17], trajectory 0 excluded, sim-WiFi-synthesized caveat), MSILN site1/B1 (cross-session real-world headline, Nov-24 train / Nov-25 val / Dec-05-06 test), UJIIndoorLoc (WiFi-only per-leg, 520 APs, canonical train + validation), RoNIN canonical (IMU-only per-leg, 32 unseen-subject sequences); only in-scope WiFi+IMU advertised for Webots/MSILN (camera/odom withheld); (2) ADDED a datasets summary Table~\ref{tab:datasets} (table*, label tab:datasets) listing dataset / modalities / role / train / val / test / APs — counts VERIFIED by running src.pipeline.data.dataset_stats in the project venv (webots 11/3/3 paths; msiln 94/34/5 paths; uji 19937/1111 samples, 520 APs; ronin 73/-/32 sequences); the split-unit mixing (paths vs samples vs sequences) is stated explicitly in the caption and flagged [[VERIFY]] in a comment; MSILN AP count not exposed by dataset_stats so left blank; (3) wrote the baselines prose — wlan_localization (WiFi SOTA) + RoNIN ResNet1D (IMU SOTA); WiFi-kNN DROPPED entirely per director decision; (4) implementation prose referencing Table~\ref{tab:train} (this subsection) and Table~\ref{tab:arch} (Methodology), hardware NVIDIA Quadro P4000, fixed seed 42, PyTorch; (5) metric-policy prose — MAE + raw ATE, Umeyama-aligned ATE only in the limitations discussion. The existing tab:train table and its % src: / % TODO reconcile-K comments left intact; the \textcolor{red} Experiments-roadmap note and all other scratch subsections untouched; preamble / title block / \ifblind / \input untouched. ONE small edit in paper/draft.tex: removed the WiFi-kNN mention from the Experimental setup baselines bullet (now wlan_localization + RoNIN ResNet1D only); draft fusion/limitations bullets that mention kNN left alone (not in this pass). Every factual claim carries a % src: comment tracing to scope.md / notebooks/paper_results.ipynb §1 / docs/SOTA_BASELINES.md / CLAUDE.md. Stayed within scope (WiFi+IMU, single set-transformer, 4 datasets). Could NOT compile — no LaTeX engine on this machine; verified by reading that tab:train / tab:arch / tab:datasets \ref all resolve and the tab:train block is intact. | Pending author/director review |
| 2026-06-01 | 4. Experiments — Experimental Setup subsection ONLY (paper/main.tex) + standing style rule (paper-workspace/style-anti-ai.md) | Director-take revision pass on the existing Experimental Setup prose + tab:datasets; no results numbers invented. TAKE 1 (cut project-internal trivia): removed the trajectory-0 housekeeping ("trajectory $0$ is an empty collection and is excluded") — Webots is now stated simply as "co-recorded WiFi and IMU trajectories, split by trajectory into 11/3/3"; rephrased the UJI "canonical training set / separate validation set" code-source phrasing into reader-facing terms ("it provides WiFi scans without trajectory structure, so the encoder is trained and scored on the benchmark's own train and validation scans"); kept the two genuine scientific caveats — synthesized-WiFi-is-optimistic and the MSILN cross-session protocol — untouched. Recorded a STANDING STYLE RULE under a new "Reader-facing only (no project-internal housekeeping)" heading in paper-workspace/style-anti-ai.md so this class of detail (excluded/empty collections, internal renames, code fixes, plumbing, "canonical training set" phrasing) is excluded going forward. TAKE 2 (rework tab:datasets): fixed the conflated split UNITS by adding a "Unit" column (trajectories for Webots/MSILN, scans for UJIIndoorLoc, sequences for RoNIN) so 11 trajectories and 19937 scans are no longer presented as the same count; added a "Setting" column (Simulation / Real / Real cross-session for MSILN); dropped the "Role" column (role is in prose) and kept Modalities + APs; rewrote the caption to state the unit/setting columns and removed the old "split unit differs" hedge and the [[VERIFY]] comment (the Unit column resolves it). All split/AP values RE-VERIFIED by running src.pipeline.data.dataset_stats in the project venv: webots 11/3/3 trajectories (Simulation); msiln_site1_b1 94/34/5 trajectories (Real cross-session); uji_indoorloc 19937/1111 scans, 520 APs, no test split (Real); ronin_canonical 73/-/32 sequences (Real). MSILN AP count not exposed (n_aps=None) — left as em-dash with a % src note. Edited ONLY the \subsection{Experimental Setup} region of main.tex; tab:train and its % src:/% TODO comments untouched; other subsections, \textcolor notes, preamble, title block, \ifblind, \input untouched; sections/*, draft.tex, template files untouched. Could NOT compile — no LaTeX engine on this machine; verified by reading that tab:datasets/tab:train/tab:arch \ref all resolve and the new 8-column table is well-formed (8 cols in spec + 8 cells per row). | Pending author/director review |
| 2026-06-01 | 4. Experiments — Per-Leg Encoder Validation + End-to-End Fusion subsections (full PROSE) + two result tables, paper/sections/04_experiments.tex ONLY | First publication PROSE for the two Experiments result subsections, appended after Experimental Setup, drafted with AI assistance; no results numbers invented. AI actions, all in paper/sections/04_experiments.tex (main.tex, draft.tex, sections/03_methodology.tex, template files untouched): (1) Per-Leg Encoder Validation — two-paragraph prose plus lead-in, in the §3 / Experimental-Setup register: WiFi-Net on UJIIndoorLoc val MAE 8.58 m vs wlan_localization 15.17 m (-43.4%); IMUCNN on RoNIN canonical raw ATE 9.72 m vs ResNet1D 5.14 m (+89.2%), framed honestly as an in-domain-competitive encoder paying a cross-subject cost at a ~95x-smaller parameter budget (IMUCNN ~0.05 M vs ResNet1D 4.6 M), with the Umeyama-aligned figure explicitly deferred to the limitations discussion (NOT stated here); added Table A (\label{tab:perleg}, columns Modality | Dataset (split) | Metric | SOTA | Ours | Change), \ref'd from the prose before it appears, with % src on every cell. (2) End-to-End Fusion — prose flowing from 5.2: Webots simulation 2-mod K=4 val 0.395 / test 0.375 m (both under the 0.5 m bar; LIVE notebook values), with a note that there is no public SOTA on the simulation; MSILN site1/B1 cross-session headline val 15.22 / test 10.89 m vs wlan_localization 21.26 / 28.31 m (-28.4% val, -61.5% test), compared ONLY against wlan_localization (WiFi-kNN absent from prose entirely); added Table 2 (\label{tab:fusion}, columns Dataset (split) | wlan_localization | Ours | Change; Webots test 0.375 with em-dash SOTA/Change), \ref'd from the prose, with % src on every cell. (3) The Webots reconciliation (live 0.395/0.375 vs RESULT_06 anchor 0.469/0.517, test<val) is a COMMENT-ONLY `% TODO` next to the value — NOT in the prose. (4) NO figure float / \includegraphics added (asset undecided UJI scatter vs MSILN trajectory) — a `% TODO: fusion figure pending director choice` comment only, with no dangling \ref. Every quantitative/comparative claim carries a % src: comment tracing to notebooks/paper_results.ipynb §3/§4 live cells + RESULT_01/06/07/15/37/39 + scope.md; no [[VERIFY]] needed (all values given/live). Stayed within scope (WiFi+IMU, single set-transformer, 4 datasets; no WiFi-kNN, no camera/odom, no out-of-scope architectures). Could NOT compile — no LaTeX/detex engine on this machine; verified by reading that tab:perleg / tab:fusion / tab:datasets / tab:train / tab:arch \ref/\label all resolve and no figure float was introduced. | Pending author/director review |
| 2026-06-01 | 4. Experiments — table FORMATTING only, tab:perleg + tab:fusion (paper/sections/04_experiments.tex ONLY) | Director-take table-formatting fix on the two methods-comparison tables; NO numeric value, prose, label, or `% src:` comment content changed. ISSUE 1 (single-column fit): tab:perleg (Per-Leg Encoder Validation) overflowed the 7.5 cm text column at 6 columns. Made it fit one column (kept single-column `table`, did NOT convert to `table*`): dropped the redundant "Modality" column (the dataset+metric already imply it — UJIIndoorLoc/MAE => WiFi, RoNIN/raw ATE => IMU), abbreviated the "Change" header to "$\Delta$", added `\small` and `\setlength{\tabcolsep}{4pt}`; column spec now `|l|l|c|c|c|` (5 cols, 5 cells/row). tab:fusion (End-to-End Fusion) at 4 columns was tight because of the long `wlan\_localization` header — applied a minimal fix: `\small` and shortened the header to `wlanloc` with the full name spelled out in the caption (`wlanloc = wlan\_localization`); also abbreviated its "Change" header to "$\Delta$". ISSUE 2 (bold best per row, comparison tables only): bolded the lowest-error value in each row of the two comparison tables with `\textbf{...}` — tab:perleg WiFi row `\textbf{8.58}` (Ours beats 15.17), IMU row `\textbf{5.14}` (SOTA beats 9.72, bolded honestly); tab:fusion Webots-test `\textbf{0.375}`, MSILN-val `\textbf{15.22}` (beats 21.26), MSILN-test `\textbf{10.89}` (beats 28.31). No bold on any `$\Delta$` column; tab:datasets / tab:train / tab:arch left unbolded (not methods comparisons). Added "Bold marks the best (lowest-error) result in each row." to both comparison-table captions. Every numeric value, all prose, both `\label`s, and all `% src:` comment content preserved verbatim. Edited ONLY paper/sections/04_experiments.tex; main.tex, draft.tex, sections/03_methodology.tex, template files untouched. Could NOT compile — no LaTeX engine on this machine; verified by reading that each row's cell count matches its column spec, all `\textbf{...}` are balanced, and tab:perleg/tab:fusion `\label`/`\ref` still resolve. | Pending author/director review |
| 2026-06-01 | 4. Experiments — Per-leg encoder validation + End-to-end fusion subsections ONLY (paper/draft.tex bullet outline) | Refined the bullet outline (draft register, NOT prose) for two Experiments subsections, grounding every quantitative bullet in notebooks/paper_results.ipynb live cell outputs; no numbers invented. AI actions, all in paper/draft.tex (main.tex, sections/*, template files untouched): (1) Per-leg encoder validation — replaced the approximate "~8.6 m" / "~9.7-9.96 m" hedges with the EXACT live notebook values: WiFi-Net UJI val 8.58 m vs wlanloc 15.17 m (-43.4%) from §3 cell [10]/[6]; IMUCNN RoNIN raw ATE 9.72 m vs ResNet1D 5.14 m (+89.2%) from §3 cell [11]/[8]; scope.md/RESULT anchors (8.69; 9.96/+94%) noted as seed-noise in the % src comments; Table~A bullet cites cell [12]; kept the honest in-domain-vs-cross-subject IMU framing and explicitly parked the Umeyama number for limitations. (2) End-to-end fusion — recorded the EXACT Webots notebook print (val MAE 0.395 m, test MAE 0.375 m, with the test<val relation flagged) from §4 cell [14]; RETAINED the Webots-reconciliation "Open ---" bullet (live val 0.395/test 0.375 vs scope.md/RESULT_06 anchor val 0.469/test 0.517) so the director picks the canonical run; refined the MSILN headline bullet to exact live values (val 15.22 / test 10.89 vs wlanloc val 21.26 / test 28.31, -28.4% val computed as (15.22-21.26)/21.26, -61.5% test as printed by cell [15]); per director decision REMOVED the WiFi-kNN comparison bullet (the stale 17.66/9.47, and the notebook's actual kNN 12.02/6.94) AND the "WiFi-kNN beats fusion on test (path-130)" honest-note bullet from this subsection — comparison is now against wlan_localization only; refined the figure bullet (notebook currently renders GT-vs-SOTA-vs-Ours as a UJI val scatter, cell [20]; flagged the MSILN-trajectory-figure alternative) and re-pointed it + the headline to Table~2 (cell [21]). Every quantitative bullet carries a % src: comment naming the exact notebook cell and RESULT id; no [[VERIFY]] needed (all values traced to live cell outputs). The Webots-open and K-sweep-open items left unresolved per instruction. Stayed within scope (WiFi+IMU, single set-transformer, 4 datasets). Bullets-only (draft register), no prose. Could NOT compile — no LaTeX engine on this machine; verified by reading. | Pending author/director review |
| 2026-06-01 | 3. Methodology + 4. Experiments (paper/sections/03_methodology.tex + paper/sections/04_experiments.tex ONLY) | Length-reduction / tightening pass (levers A+B+C) to recover ~one column; NO scientific content, number, equation, or rigor removed. LEVER A (de-dup forward pass, §3.4): the forward pass was described three times (prose + Algorithm 1 + equations). Kept all equations (eq:layer/eq:mhsa/eq:readout); SHRANK Algorithm 1 from ~30 lines to a 3-step compact form (one CLS-prepended token-set assembly line citing Eq.~token, the L-layer loop citing Eq.~layer/mhsa, one fused readout line citing Eq.~readout) — kept `\label{alg:forward}` and the `Algorithm~\ref{alg:forward}` sentence (rewritten from a restating "states the forward pass end to end, from ... through ... to ..." to "assembles these steps into the end-to-end forward pass"); CUT the duplicated closing paragraph that restated permutation-invariance / "modality added or removed by inserting or deleting tokens" (already stated once in §3.1), leaving only the readout-specific CLS NaN-safety sentence. LEVER B (prose tightening, §3 and §4): shortened wordy sentences and removed residual redundancy throughout — §3.1 Overview (2 paragraphs condensed), §3.2 (cut "unaligned and of unequal rate" duplicate clause, merged the two degradation-condition sentences), §3.3 lead-in + WiFi-Net + IMUCNN + §3.4 fusion opener (cut second "cross-modal==cross-time same operation" restatement, already in §3.1) + MHSA/CLS + readout, §3.5 training opener + dropout prose; §4 Experimental Setup (Webots/MSILN/UJI/baselines/metrics paragraphs), Per-Leg Validation, End-to-End Fusion. Every claim, number, caveat, and `% src:` comment preserved. LEVER C (caption trims): tab:datasets caption cut from ~7 lines to 2 (kept the Unit-column explanation); tab:perleg and tab:fusion captions tightened (KEPT the "Bold marks the best (lowest-error) result in each row." note in both); tab:arch caption shortened; the Figure 1 caption descriptive sentence tightened while KEEPING the `\textcolor{red}{text is not visible: figure to be enhanced}` note. PRESERVED EXACTLY: all numeric/architectural values (D=128, M=2, N_a=64, T=32, F=9, channels (32,64,128), L=6, H=4, r=4, n=32, periods [0.05,120] s, ~0.05 M params; 8.58, 15.17, 9.72, 5.14, 0.395, 0.375, 15.22, 10.89, 21.26, 28.31; 43.4/89.2/28.4/61.5 %; 520 APs; splits 11/3/3, 94/34/5, 19937/1111, 73/32; lr 1.3e-3, wd 1e-4, Huber δ=0.5, grad-clip 1.0, 90 epochs, B=128, K=4, seed 42, p_m=0.4, p_i=0.45); all equations + their `\label`s (eq:token, eq:time, eq:wifi_assign, eq:wifi_token, eq:imu, eq:layer, eq:mhsa, eq:readout); all `% src:` comments; all bold best-result cells in tab:perleg/tab:fusion; all `% TODO` markers (K-reconcile ×2, Webots reconcile, fusion-figure) and all `\textcolor{red}` notes; scope (single PositionQuery readout, WiFi+IMU, 4 datasets). Edited ONLY the two section files; main.tex, draft.tex, template files untouched. All `\ref`/`\label` verified by reading (alg:forward, fig:pipeline, eq:*, tab:arch/train/datasets/perleg/fusion all resolve). Could NOT compile — no LaTeX engine on this machine; verified by reading. The actual one-column saving can only be confirmed on a real compile. | Pending author/director review |
| 2026-06-01 | 3. Methodology — §3.3 Per-Modality Encoders (WiFi-Net, IMUCNN) ONLY (paper/sections/03_methodology.tex) | Inserted two author-made encoder figures as single-column floats (`figure`, NOT `figure*`) and wired minimal text references; no prose rewritten, no numbers/equations/claims changed. AI actions: (1) added `\begin{figure}[t]` with `\includegraphics[width=\columnwidth]{figures/figure_2.jpg}`, `\label{fig:wifinet}` at the top of the WiFi-Net subsubsection (caption: "WiFi-Net: an RSSI scan is scored against the $N_a$ learned anchors; a softmax assignment weights the anchor embeddings into a token, refined by a residual MLP."), depicting the WiFi-Net mechanism of Eqs.~eq:wifi_assign/eq:wifi_token (wifi.py:43-72,86-97); referenced it by appending "(Figure~\ref{fig:wifinet})" to the existing anchor-prototype sentence — one clause, no new paragraph. (2) added `\begin{figure}[t]` with `\includegraphics[width=\columnwidth]{figures/figure_3.jpg}`, `\label{fig:imucnn}` at the top of the IMUCNN subsubsection (caption: "IMUCNN: three 1-D convolutional blocks over the inertial window, global average pooling to a pooled vector, and a linear projection to the token."), depicting the IMUCNN mechanism of Eq.~eq:imu (imu.py:55-78); referenced it by appending "(Figure~\ref{fig:imucnn})" to the existing dense-motion-over-a-temporal-window sentence — one clause. (3) added a `% src:` comment under each float naming the asset file + the eq/code it depicts. PRESERVED EXACTLY: all equations + labels (eq:token, eq:time, eq:wifi_assign, eq:wifi_token, eq:imu, eq:layer, eq:mhsa, eq:readout), all `% src:` comments, the Figure 1 (`fig:pipeline`) float + its red caption note, all `\textcolor{red}` notes, all `% TODO` markers, every number, and scope (single PositionQuery, WiFi+IMU). Both new labels are referenced; all existing `\ref`/`\label` still resolve. Edited ONLY paper/sections/03_methodology.tex; main.tex, draft.tex, 04_experiments.tex, template files untouched. Could NOT compile — no LaTeX engine on this machine; verified by reading (floats well-formed, single-column, refs resolve). | Pending author/director review |
| 2026-06-17 | 4. Methodology (paper/sections/04_methodology.tex ONLY) | Refine-and-verify pass (NOT a rewrite) over the orchestrator's revised Methodology, owning the prose to the anti-AI / ICINCO register. No structural or mathematical change: every equation, label, algorithm step, and the $f=h\circ g$ / loss visibility added by the orchestrator were preserved. AI actions, prose-only: (1) tightened the Overview opener (folded the "WiFi sparse / IMU dense" restatement into a pointer to Section~\ref{sec:problem}, since the Problem Statement already opens with it — concision cut #3), and re-anchored the decomposition mention to Equation~\eqref{eq:decomp}; (2) trimmed the tokenization opener's trailing duplicate sentence and de-duplicated "elapsed time" wording around eq:token; (3) removed the second "without resampling" / "whatever its rate" echo in the continuous-time closing; (4) replaced WiFi-Net's editorial "soft assignment matches WiFi's role" sentence with a single non-redundant fact (reference points are learned, not tied to physical APs — concision cut #2); (5) merged the IMU shift-invariance triple-statement to two distinct claims — conv blocks shift-equivariant, GAP turns that into shift-invariance (concision cut #1); (6) split two-idea sentences in the fusion opener, the MHSA/padding paragraph, and the loss list for one-idea-per-sentence; (7) trimmed the readout closing so the CLS NaN-safety is stated once (it was already in the MHSA paragraph) while keeping the eq:perm permutation-invariance tie. Verified every numeric/architectural claim against model code: eq:token/eq:time vs transformer.py:53-66,220-222; eq:wifi_assign/eq:wifi_token vs wifi.py:56-72,88-96 (temperature kept as $\gamma$); eq:imu_conv/gap/imu vs imu.py:22-98 (F=9, T=32, channels 32/64/128); eq:layer/eq:mhsa vs TransformerEncoderLayer pre-norm/GELU/ff=4D; eq:readout vs cross-attn query over [CLS||tokens] with $\phi(\tau)$; L=6/H=4 per fusion.yaml (config overrides code defaults). One flag: the closing "~1.5 M parameters" is generous vs the measured 1,380,035 (~1.38 M) full 2-mod model; kept verbatim per the task's hard constraint and listed as [[VERIFY]]. All 15 labels (sec:method, subsec:tok, subsec:fusion, alg:forward, eq:token/time/wifi_assign/wifi_token/imu_conv/imu_gap/imu/layer/mhsa/readout/loss) and the five problem-statement cross-refs (eq:decomp, eq:spaces, eq:perm, eq:objective, sec:problem) resolve; all displayed equations kept column-safe (loss = gathered+cases, layer = aligned, untouched). Char count 12,121 -> 11,907 excl. spaces (-214). Edited ONLY 04_methodology.tex (+ PAPER_STATE.md, ai-usage.md). Could NOT compile — no LaTeX engine on this machine; verified by reading + balance grep (eq 11/11, cases 1/1, gathered 1/1, aligned 1/1, algo 1/1). | Pending author review |
| 2026-06-17 | 5. Experiments + 6. Results (paper/sections/05_experiments.tex + paper/sections/06_results.tex ONLY) | Length-reduction / de-duplication pass at the supervisor's request (both sections flagged too large); shrink ~10% from EACH by eliminating redundancy and over-explanation only — NO fact, number, citation, table, figure, or honest caveat deleted. Counts: 05_experiments.tex 606 -> 547 words (3878 -> 3559 nospace, -9.7%); 06_results.tex 1346 -> 1215 words (9239 -> 8483 nospace, -9.7%). Cites unchanged (Experiments 8 \cite occurrences, Results 2). Redundancies cut, Experiments: (1) trimmed the Datasets opening sentence that recapped the table's columns (setting/modalities/AP count/splits) down to "settings and splits"; (2) DELETED the trailing "split unit differs across datasets..." paragraph entirely — the tab:datasets caption already explains the Unit column; (3) condensed each Reference-Methods bullet's "It is the X reference for Y" tail into a semicolon clause (role mapping kept verbatim). Redundancies cut, Results: (4) the cross-session story was told 2-3 times — kept the single "wlan_localization nearly doubles its error from validation to test, the fusion error falls because..." sentence, folded the separate IMUWiFine "fails to generalize across sessions" + "its training curve confirms it never fits the cross-session mapping" into one clause ("stays above $52$~m on both splits (Fig.), an informative negative result..."); (5) removed recap-after-figure prose on fig:ronin_traj (merged "shows the consequence... IMUCNN tracks the route but drifts more" into the gap sentence as "with the drift visible on one held-out sequence in Fig."), fig:msiln_cdf / fig:msiln_perpath (now "The full per-sample test error distribution is in Fig.; per-path error... in Fig."), and fig:iwfine_showcase (dropped "compares wlanloc, the IMUWiFine LSTM..., and the proposed method on four paths" — the caption says it); (6) trimmed the RoNIN per-sequence paragraph's repeat of the aggregate-gap framing ("The lighter encoder is competitive sequence by sequence, and the 89.2% aggregate gap..." -> "The 89.2% gap thus comes from a few harder sequences"); (7) dropped the UJI "under that constraint WiFi-Net roughly halves the reference error" (restates -42.7%); (8) dropped the modality-dropout closing recap ("Fusion degrades gracefully rather than failing when one stream is absent"); (9) compressed Limitations to the concession, not the re-derivation ("the residual reflects a $95\times$-smaller encoder paying a cross-subject generalization cost, and closing it..." -> "closing it at the $95\times$-smaller budget is open work"); (10) tightened subsection openers (fusion "into a single position estimate" removed; staleness "Letting the WiFi token grow stale" -> "Aging the WiFi token"; generalization "advantage to exploit" -> "advantage"). HONEST CAVEATS PRESERVED VERBATIM in substance: synthesized Webots WiFi is optimistic; MSILN cross-session protocol; test split dominated by one WiFi-dense path (786 samples, ~28%). EVERY protected number verified present by grep: staleness chain 0.61->1.13->1.48->1.84->2.05 and 3.50; 8.69/15.17/-42.7; 9.72/5.14/+89.2; 0.05M/4.6M/95x; 10.90/28.31/12.49/52.69/-61.5/-12.7/-79.3; 16.67/16.88/21.26/65.15; 0.61 sim; 6.37/2.31/1.06/0.84; +48.2/7.62; 4.77 ms/0.146 ms; 786/~28%; 0.86 WiFi-only. All labels (sec:experiments, sec:results, eq:mae, eq:ate, tab:datasets, tab:perleg, tab:fusion, fig:ronin_traj, fig:ronin_perseq, fig:webots_scatter, fig:msiln_curves/cdf/perpath/msiln, fig:iwfine_showcase) and all 7 \includegraphics preserved; no \cite added or removed. No em-dash introduced (matched the sections' comma/colon convention). Edited ONLY the two section files (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by wc/grep + reading. | Pending author/supervisor review |
| 2026-06-18 | Abstract (paper/main.tex `\abstract{}`) + 6. Results End-to-End Fusion (paper/sections/06_results.tex) + 2. Related Work final-properties paragraph (paper/sections/02_related_work.tex) | Claim-calibration pass (one focused iteration) recasting THREE claims to match the measured margins; no number, table, figure, label, equation, or the `\ifblind`/author block touched. EDIT 1 (abstract verbs): rewrote the closing sentences so they lead on the conceptual contribution + graceful degradation, state dead reckoning honestly ("improves on dead reckoning on the test session and matches it on validation" — test 10.90 vs 12.49 m, val 16.67 vs 16.88 m essentially tied), state that the model "outperforms a learned WiFi--IMU fusion that fails to transfer across sessions" (IMUWiFine LSTM, test 10.90 vs 52.69, val 16.67 vs 65.15), and demote the wlan_localization k-NN gap to "only a sanity check" rather than the headline (test 10.90 vs 28.31, -61.5%). Length-neutral: abstract recounted at 199 words (was 184; ceiling 200) — trimmed iteratively to stay under the cap. All facts already traceable in 06_results.tex; no new number introduced. EDIT 2 (k-NN reframe, End-to-End Fusion): added ONE calibrating sentence after the IMUWiFine negative-result sentence — "Out-performing wlan_localization, a distance-weighted WiFi k-NN with no motion model, is expected for any method that tracks motion, so the -61.5% figure is a sanity check; the informative comparisons are against PDR-from-start and the IMUWiFine LSTM." No number/caveat deleted; the -61.5/28.31/10.90 values and the per-ENCODER UJIIndoorLoc 8.69/15.17/-42.7 result left untouched (different claim). EDIT 3 (STraTS delineation, Related Work): added ONE sentence right after the SeFT/STraTS "but for clinical data" note pinning the delta beyond domain shift — (i) per-modality encoders mapping heterogeneous WiFi RSS scans + inertial windows into the shared token (STraTS assumes scalar clinical observations), (ii) modality + per-instant dropout for cross-session robustness (STraTS does not address). No hype word ("novel" avoided). Anti-AI self-check run: zero banned vocab (grep-confirmed), <=1 tricolon/paragraph, no recap-after-figure, one idea per sentence (the EDIT-3 two-part delta kept as one sentence per the task's explicit "ONE crisp sentence" instruction). Brace/environment balance verified unchanged by reading + script (main.tex {47/47} begin/end 1/1; 06_results {89/89} 12/12; 02_related_work {20/20} 0/0). Edited ONLY main.tex, 06_results.tex, 02_related_work.tex (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/supervisor review |
| 2026-06-18 | 2. Related Work (paper/sections/02_related_work.tex) + 3. Problem Statement (paper/sections/03_problem_statement.tex) | Reader-aids pass (one iteration) adding THREE reviewer-requested aids; no new scientific number — every addition restates a fact already in the paper. No equation, existing table/figure/number, abstract, or `\ifblind`/author block touched. ADD 1 (Min2, Related Work): generated a new full-width Table~\ref{tab:properties} (`\label{tab:properties}`, table*, `|l|l|c|c|c|`) scoring 7 method assemblies — iMoT, AFT-VO, ContiFormer, SeFT/STraTS, WIO-EKF, Deep concat fusion, Ours — against the three organizing properties already named in the section: P1 (continuous-time tokens without resampling or an ODE solver), P2 (one permutation-invariant set transformer for cross-modal+cross-time fusion in a single self-attention block), P3 (learned missing/stale robustness validated across sessions); 3-state marks \checkmark / $\sim$ / -- defined in the caption (\checkmark needs amssymb, already loaded). A one-sentence lead introduces and \ref's the table; the former per-method enumeration prose was FOLDED into the table (removed, no recap-after-table); the "Ours completes the combination" closing claim was kept. Each row carries a `% src:` to the section's own prose, and every mark was VERIFIED against that prose (ContiFormer P1=$\sim$ ODE-in-attention, P2=-- single-stream; WIO-EKF P3=$\sim$ ten-day session gap but filter-based; Deep concat P3=$\sim$ hand-set null vector). ADD 2 (Min4, Problem Statement): generated a new full-width Table~\ref{tab:notation} (`\label{tab:notation}`, table*, `|l|l|l|l|`) — a Symbol/Meaning glossary laid out as two pairs per row (18 rows, 36 symbols) covering every symbol the task listed across the Problem Statement and Methodology, each row meaning-matched to the prose and `% src'd` to its defining equation. \ref'd once near the top ("Table~\ref{tab:notation} collects the notation."). Bare $\tau$ (the query offset in eq:readout) was deliberately EXCLUDED to avoid clashing with $\boldsymbol{\tau}$ (the period vector in eq:time); only the period range $[\tau_{\min},\tau_{\max}]$ is listed. Forward \eqref's into 04_methodology (eq:token, eq:time, eq:wifi_assign, eq:loss) all resolve in-document. ADD 3 (Min6, Problem Statement): made staleness quantitative at first use — the definition now reads "an observation is stale once $|\Delta t_i|$ exceeds the modality's nominal update interval, about one second for the wifi stream against roughly 30 Hz for the IMU" and adds that the Section~\ref{sec:results} staleness sweep ages the wifi token from fresh to fully stale across the $K$ held instants while the IMU stays current. No seconds-grid or slope number invented; `% src` points to the CLAUDE.md sensor-rate table (wifi ~31 sim steps x 32 ms ~ 1 s; IMU ~30 Hz). Anti-AI self-check: zero banned vocab, <=1 tricolon/paragraph, no recap-after-table, one idea per sentence. Brace/environment balance verified by script (02_related_work {30/30} table* 1/1 tabular 1/1; 03_problem_statement {141/141} table* 1/1 tabular 1/1); per-row column counts verified (properties 5, notation 4); both new labels have exactly one \ref each and no name collision (grep). No [[VERIFY]] raised. Edited ONLY the two section files (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/supervisor review |
| 2026-06-23 | Abstract + 1. Introduction + 2. Related Work (front matter) + 3. Problem Statement light polish (paper/main.tex, paper/sections/01_introduction.tex, 02_related_work.tex, 03_problem_statement.tex) | Accept-and-de-color pass over supervisor (co-author) Overleaf `{\color{blue}...}` edits extracted at _nav3_extract/; integrated his rewordings into the working files, de-colored (no `\color`/`\textcolor` introduced; current files already carried none), and rectified non-native grammar to clean scientific English with anti-AI self-check. No number, citation, equation, label, table, or figure float changed. ABSTRACT (main.tex `\abstract{}`): took ONLY the supervisor's SETUP-half rewordings — first-use "WiFi received-signal-strength-indicator (RSSI) fingerprints", "sampled asynchronously at very different rates", "so at any instant some information is missing or stale", elapsed time glossed "from acquisition to the localization query", token carries "the sensor data, its modality, and a learned encoding of its elapsed time" (makes elapsed time + data type explicit inputs), "evaluate ... on both simulated and real data"; fixed his clumsy phrasings ("resulting that", "without resampling nor", "(WiFi or IMU )"). DELIBERATELY did NOT adopt his over-claiming results half — the calibrated honest second half ("degrades smoothly ... improves on dead reckoning on the test session and matches it on validation ... outperforms a learned WiFi--IMU fusion that fails to transfer across sessions; beating a distance-weighted WiFi k-NN is only a sanity check ...") preserved verbatim in meaning. Trimmed filler elsewhere to hold the 200-word ceiling: recounted at exactly 200 words (whitespace count over the brace-matched `\abstract{}` body). INTRODUCTION (01_introduction.tex): framed indoor difficulty around the lack of an effective global reference system (noisy measurements; ambiguous, complex indoor environment) with the error-prone sentence grammar fixed; consolidated the WiFi/IMU complementarity paragraph (WiFi = sparse absolute reference, IMU = dense relative motion, complementary yet heterogeneous and asynchronously sampled, either may drop out or arrive stale), IMU acronym defined here at first use; "In this paper, we address this challenge by proposing ..."; roadmap fixed to "The remainder of this paper is organized as follows:". Figure~\ref{fig:pipeline} float kept EXACTLY in place (not moved/deleted). RELATED WORK (02_related_work.tex): applied the supervisor's first-three-paragraph rewordings — "WiFi RSSI fingerprints recover absolute indoor position by matching a live scan to a labelled survey"; "On UJIIndoorLoc, a 1-NN reference method reports a 7.9 m error" + "such fingerprinting stays single-modality, ..."; "Learned inertial navigation provides dense relative estimates."; "attention entered the field through iMoT, which uses accelerometer and gyroscope channels"; "with no absolute reference, inertial tracking drifts; combining WiFi's absolute reference with inertial motion is the remedy."; "The fusion of these two heterogeneous streams is well studied, but ..."; fixed his grammar ("can provide a type of dense estimates", "the inertial tracking drifts exist"). Kept British spelling (labelled / generalisation / localisation); did NOT switch to US. The comparison table tab:properties and all `\cite` untouched (13 cite groups, 7.9 m intact). PROBLEM STATEMENT (03_problem_statement.tex): LIGHT grammar/flow polish only — de-duplicated the IMU acronym (now defined once, in the Introduction) and smoothed one transition ("; an inertial measurement unit (IMU) reports" -> ", whereas an IMU reports"). NO equation, `\label`, or structure changed: eq:spaces, eq:obsset-gen, eq:objective-gen, eq:obsset, eq:objective, eq:fit, eq:decomp, eq:perm all left exactly as-is (label set verified identical). Anti-AI self-check: zero banned vocab (grep-confirmed across all four files), <=1 tricolon/paragraph, varied paragraph openings (no 3 "We" in a row), no recap-after-figure (the Figure~1 ref sits inside the contribution sentence; the following text does not describe the figure), first sentences do not restate section headers. Brace/begin-end balance verified by script (main {47/47, 1/1}; intro {16/16, 1/1}; related {24/24, 2/2}; problem {116/116, 13/13}); no `\color`/`\textcolor` in any of the four; no duplicate `\label` across them. Could NOT compile — no LaTeX engine on this machine; verified by reading + grep + word-count scripts. Edited ONLY the four files (+ PAPER_STATE.md, this file). | Pending author/supervisor review |
| 2026-06-23 | 4. Methodology + 6. Results (paper/sections/04_methodology.tex + paper/sections/06_results.tex ONLY) | Style/readability pass at director request ("simplify when possible but stay fancy in the scientific language"): (1) reduce hyphenated "x-y" compounds, (2) lighten the densest sentences while keeping a polished scientific register. No number, percentage, metric value, or claim changed; no `\label`/`\cite`/`\ref`/`\eqref`/equation/table/figure/`\includegraphics`/`\subref` added, removed, or renamed; the paper `\title` untouched; no `\color`/`\textcolor` introduced. HYPHEN REWORDINGS (04): `set-transformer`->"set transformer" in prose (the \subsection{Set-Transformer Fusion...} heading kept as allowed); subsection "...and Modality-Specific Encoders"->"...and Encoders for Each Modality"; "no modality-specific branches"->"no branch per modality"; "a modality-specific encoder realizes $g$"->"An encoder for each modality realizes $g$"; "the modality-specific encoder output"->"the output of the encoder for that modality"; "without modality-specific wiring"->"without wiring specific to its modality"; "place-discriminative distribution"->"distribution that discriminates place"; "reference-point matrix"->"matrix of reference points"; the "assignment-weighted aggregation of the reference-point embeddings ... refined by a residual layer-normalized MLP" unstacked into "The weights $\mathbf{w}$ aggregate the embeddings of the reference points ... into a pooled vector, which a residual MLP with layer normalization then refines into the token"; fig:wifinet caption "reference-point embeddings"->"embeddings of those reference points"; "dense short-term motion ... relative-motion complement"->"dense motion over a short temporal window, supplying the relative motion that complements WiFi's absolute reference"; "capture short-term dead reckoning"->"capture dead reckoning over short intervals". HYPHEN REWORDINGS (06): "Modality-Specific Encoder Evaluation" subsection+caption->"Encoder Evaluation by Modality"; "fingerprint-to-position mapping"->"mapping from fingerprint to position"; "$32$ held-out sequences"->"$32$ sequences held out from training"; "one held-out sequence"->"one such sequence"; "five best held-out sequences"->"five best sequences"; "less one-sided per-sequence picture"->"more even picture sequence by sequence"; "the set-transformer fuses"->"the set transformer fuses"; "cross-session real-world collection"->"real collection across sessions"; "the two-modality model"->"the model fusing the two modalities"; "This sub-metre figure"->"This figure, below one metre"; "real-world numbers"->"real numbers"; "real, cross-session evaluation"->"real evaluation across sessions"; "On the held-out test session"->"On the test session, held out for evaluation"; "Out-performing"->"Beating"; "full per-sample test error distribution"->"full distribution of test error per sample"; "per-path error against PDR-from-start"->"error for each path against PDR-from-start"; tab:fusion caption "two-modality reference; ... is cross-session"->"reference fusing two modalities; ... runs across sessions"; subfig captions de-hyphenated ("Per-sample"/"Per-path"->"per sample"/"for each path"); webots caption "sub-metre"->"below one metre"; "within-distribution held-out-path split ... session-drift advantage"->"a split that holds out paths within the same distribution ... gains nothing from session drift"; "the cross-session MSILN test"->"the MSILN test across sessions"; iwfine caption "per-path test MAE"->"test MAE for each of four paths"; "the IMU-only level"->"the level of IMU alone". KEPT (standard ML, per task): self-attention, feed-forward, multi-head, pre-norm, real-valued, permutation-equivariant/invariant, cross-modal, cross-time, scaled-dot-product, continuous-time, ground-truth(modifier), NaN-safe, cross-subject, distance-weighted, WiFi-dense, real-time, WiFi--IMU, end-to-end, and proper names WiFi-Net, IMU-CNN, PDR-from-start; one fig:ronin_traj caption "held-out sequence" left as a self-contained split-name. DENSE SENTENCES LIGHTENED: (04) the permutation-equivariant comma-splice split into two sentences; the $\phi$ sinusoid run-on broken at the projection; the readout closing semicolon-splice split; the loss-pointer stacked subject unstacked. (06) the in-domain-competitive jargon stack split + de-jargoned; the "fusion error falls, because fusing..." run-on split; the within-distribution split sentence unstacked. Anti-AI self-check: zero banned vocab (grep), <=1 tricolon/paragraph, no recap-after-figure, varied openings, one idea per sentence. INTEGRITY (grep + reading; no LaTeX engine on this machine): all 30 protected 06 numbers identical count before/after; 04 braces 299/299 begin/end 17/17, 06 braces 92/92 begin/end 12/12; zero `\color`; every `\label`/`\ref`/`\eqref`/`\cite`/`\includegraphics`/`\subref` key identical (04 = 42 keys; 06 = 23 keys + 3 subref). Edited ONLY the two section files (+ PAPER_STATE.md, this file). | Pending author/supervisor review |
| 2026-06-01 | 4. Experiments (paper/draft.tex ONLY — the brainstorming bullet outline) | Drafted the Experiments section as a BULLET OUTLINE (not prose), replacing the `\section{Experiments} TODO` placeholder, mirroring the existing Methodology bullet register in draft.tex. main.tex, sections/*, and template files untouched. AI actions: (1) added a top-of-section comment block listing the five OPEN DECISIONS for the director (Webots 0.375 vs 0.517 reconciliation; 2-mod K-sweep vs 4-mod-with-caveat vs drop + fusion.yaml:71 n_instants=8 vs reported K=4; metric-policy drift — RMSE/Umeyama placement; confirm WiFi-kNN as the second MSILN baseline; reproducibility wrinkles — "PerCom 2026" header should be ICINCO, ResNet1D 5.14 + wlanloc-MSILN cached literals, IMUCNN inline-train uses ronin_a000_intra while labeled canonical); (2) six subsections of itemized bullets — Experimental setup (4 datasets, splits, baselines, implementation/hardware, metric policy), Per-leg encoder validation (WiFi-Net 8.6 m vs wlanloc 15.17 m -43%; IMUCNN raw ATE ~9.7-9.96 m vs ResNet1D 5.14 m +~90%), End-to-end fusion (Webots 2-mod K=4 with the 0.375/0.517 Open bullet; MSILN headline val 15.22 / test 10.89 vs wlanloc 21.26/28.31 = -28%/-62% + WiFi-kNN 17.66/9.47; path-130 honest note), Ablations and robustness (modality dropout 0.495/3.77/0.375; staleness cliff->slope ~0.029 m/s R^2~0.998; K-axis Open bullet; latency ~5.1 / ~0.17 ms), Discussion and limitations (smoothness debt visual; IMU cross-subject gap raw +~90% / Umeyama ~7.6 m +~48-53%; MSILN path-130 composition), and a Closing-paragraph recap bullet. Every quantitative bullet carries a `% src:` comment tracing to a RESULT id and/or notebook section and/or scope.md. Two unresolved values written as visible "Open ---" bullets (Webots 0.375 vs 0.517; 2-mod K-sweep + n_instants reconciliation) rather than silently picked, matching the Methodology draft convention. No numbers invented — all verified against scope.md §6, RESULT_01/06/07/14/15/37/39, and configs/stage_c/fusion.yaml:71. Stayed within scope (WiFi+IMU, single set-transformer, 4 datasets; no camera/odom/IPIN/IMUWiFine/TartanAir; no CNN1D/LSTM-attn/MoTTransformer). Bullets-only (draft register), no prose. Could NOT compile — no LaTeX engine on this machine; verified by reading. | Pending author/director review |
| 2026-06-23 | 5. Experiments + 6. Results (paper/sections/05_experiments.tex + paper/sections/06_results.tex ONLY) | Light consistency + clarity + grammar pass projecting the supervisor's front-matter editing style (introduce/observe acronyms on first use, tie into the problem-statement notation where natural, clean phrasing) onto the two sections he did not touch. No number, percentage, table cell, claim, `\label`, `\cite`, equation, table, figure, or `\includegraphics` changed; no `\color`/`\textcolor` introduced. FOUR small edits: (1) EXPERIMENTS terminology — observed the `$k$-NN` shorthand at its definition in the wlan_localization reference-method bullet ("$k$-nearest-neighbour ($k$-NN) RSSI fingerprinting"), so the `$k$-NN` used later in Results is introduced on first use; RSSI/IMU/metric names ("raw ATE"/"MAE"/"Umeyama-aligned ATE") were already consistent (grep-confirmed, no bare "RSS"). (2) RESULTS grammar — fixed a copular mismatch, "The gap is an in-domain-competitive encoder paying..." -> "The gap reflects an in-domain-competitive encoder paying..." (a gap is not an encoder); all words/numbers kept. (3) RESULTS notation tie-in (single light touch) — the Robustness opener now ties its two ablations to the problem-statement degradations: "...suppressed or aged on demand, realizing the missing ($a_i=0$) and stale (large $|\Delta t_i|$) conditions defined in Section~\ref{sec:problem}", using the established symbols $a_i$/$\Delta t_i$ verbatim and a `\ref` that resolves to the existing `sec:problem` label. (4) RESULTS register — casual sentence-opener "So the LSTM is..." -> "The LSTM is therefore strong within its own distribution...". No notation tie-in forced into Experiments (the $K=4$ already reads in the problem-statement sense). Anti-AI self-check: zero banned vocab, <=1 tricolon/paragraph, no recap-after-figure, varied openings. Integrity verified by grep: all 30 protected numbers identical count before/after; environments balanced (05_experiments 5/5, 06_results 12/12); braces 54/54 and 91->92/92 (one added balanced `\ref` pair); zero `\color`; every `\label`/`\cite`/`\ref`/`\includegraphics` key unchanged. Edited ONLY the two section files (+ PAPER_STATE.md, this file). The pending separate results-number revision was NOT touched, by instruction. Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/supervisor review |
| 2026-06-25 | 1. Introduction (paper/sections/01_introduction.tex ONLY) | Funnel rewrite of the Introduction with AI assistance to de-center robotics per director intent: the paper should read as about indoor localization in general because the method is device-agnostic at the PLATFORM level (it needs only WiFi scans + IMU windows, which phones, robots, and wearables all carry) and the real-data validation is pedestrian/smartphone (MSILN, IMUWiFine, RoNIN), not robot. Rewrote the section as a 4-paragraph broad->narrow funnel at the same overall length, editing ONLY prose (no equation/table/figure/label/cite/`\color` added or changed; the Figure 1 float + its `fig:pipeline` `\label` + caption left exactly in place). PARA 1 (broad): the "Mobile robots increasingly operate indoors..." opener was REPLACED ENTIRELY with a general indoor-localization opener ("Many agents that move through buildings must know where they are.") and three varied use cases with robotics NOT leading — pedestrian smartphone navigation (first), a mobile robot in warehouse logistics / facility inspection (second), a tracked asset reporting its location (third) — then GPS-fails-indoors, no effective global reference, noisy measurements, the environment shifting as people/objects move, accuracy bounded by hardware + site survey, closing "reliable indoor self-localization remains an open problem." PARA 2 (narrow to WiFi+IMU): "Two sensors are carried by nearly all of these devices: a WiFi radio and an inertial measurement unit (IMU)." — IMU acronym DEFINED here on first use; WiFi = sparse absolute reference to known access points, IMU = dense relative motion at a far higher rate; complementary yet heterogeneous and asynchronous, no common clock, drop-out/stale; "Fusing these imperfect, asynchronous signals into one position estimate is the core problem this work targets." PARA 3 (method, device-agnostic): "In this paper, we propose a continuous-time transformer (Figure~\ref{fig:pipeline}) that fuses asynchronous WiFi and IMU observations into a single $(x,y)$ estimate within one model that stays accurate when a sensor is missing or stale." + an explicit PLATFORM-agnostic claim ("not tied to a particular platform ... needs only WiFi scans and inertial windows, the same inputs a phone, a robot, or a wearable already produces ... applies to any device that carries these two sensors") + "We validate it on real cross-session data." PARA 4 (roadmap): UNCHANGED. HONESTY GUARDRAILS HELD: robotics is MENTIONED (use case #2 in para1, "a robot" in para3) but is no longer the central frame; no over-generalization — the method fuses EXACTLY two modalities (WiFi+IMU) and "device-agnostic" is scoped to the PLATFORM (phone/robot/wearable), NOT to adding modalities or general multimodal fusion; the prose now says "continuous-time transformer" (NOT "set-transformer") per the broadened title direction, while the Figure 1 caption that still reads "set-transformer" was left untouched per the "keep the figure float exactly as is" constraint. NO fabricated citation — the Introduction is cite-free before and after (grep-confirmed: 0 `\cite`); use cases are stated as motivation needing no cite. Anti-AI self-check: zero banned vocab (grep); tricolons = exactly one per paragraph where present (para1 pedestrian/robot/asset; para3 phone/robot/wearable), <=1 per paragraph; no recap-after-figure (para2 opens on "Two sensors are carried...", not on what the figure shows); one "We"-starter only (para3 closing), not 3 in a row; one idea per sentence; varied openings; no method-hype adjective. INTEGRITY (reading + grep; no LaTeX engine on this machine): braces 16/16; `\begin{figure}`/`\end{figure}` balanced and the float UNMOVED; all 7 `\ref` keys present and resolving — fig:pipeline + sec:related/problem/method/experiments/results/conclusion (all six section `\label`s confirmed to exist by repo grep); zero `\color`/`\textcolor`. Edited ONLY paper/sections/01_introduction.tex (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/director review |
| 2026-06-23 | Pass B style/readability: Abstract + Conclusion (paper/main.tex) + 1. Introduction + 2. Related Work + 3. Problem Statement + 5. Experiments (paper/sections/01_introduction.tex, 02_related_work.tex, 03_problem_statement.tex, 05_experiments.tex) | Pass B of a two-pass style/readability job (Pass A: Methodology + Results) at director request ("simplify when possible but stay fancy in the scientific language"): (1) reduce hyphenated "x-y" compounds, (2) lighten the densest sentences while keeping a polished scientific register. No number, percentage, metric value, dataset count, hyperparameter, or claim changed; no `\label`/`\cite`/`\ref`/`\eqref`/equation/table/figure/`\includegraphics` added, removed, or renamed; the `\title` untouched; no `\color`/`\textcolor` introduced. HYPHEN REWORDINGS — main.tex abstract: "received-signal-strength-indicator"->"received signal strength indicator", "WiFi-fingerprinting"->"WiFi fingerprinting"; main.tex Conclusion: "modality-specific benchmarks"->"single-modality benchmarks", "sub-metre accuracy"->"accuracy below one metre". 01_introduction: none (all hyphens keep-terms). 02_related_work: "modality-specific branches"->"a separate branch per modality"; "missing-sensor robustness"->"robustness to a missing sensor"; "cross-session generalisation"->"generalisation across sessions"; "Whole-modality dropout"->"Dropping an entire modality at training time"; "missing-channel robustness"->"robustness to an absent channel"; "per-modality encoders"->"an encoder per modality"; "per-instant dropout"->"instant dropout" (x2); "cross-session robustness"->"robustness across sessions"; closing "whole-modality dropout"->"the earlier whole-modality scheme"; "cross-session evaluation"->"evaluation across sessions". 03_problem_statement: "multi-modal"->"multimodal"; "held-out test trajectory"->"test trajectory ... held out from training"; "per-observation encoding"->"an encoding of each observation"; "modality-specific encoders"->"one encoder per modality". 05_experiments: "sub-metre accuracy"->"accuracy below one metre"; "real-world test"->reordered to "real cross-session test"; "WiFi-fingerprinting benchmark"->"WiFi fingerprinting benchmark"; "single-encoder" (x2)->"encoder-only"; "learned-inertial regressor"->"learned regressor over inertial windows"; "dead-reckoning" (noun mod)->"dead reckoning"; "cross-session comparison"->"comparison across sessions"; "set-transformer" (prose)->"set transformer". KEPT (standard/claim/proper-name): self-attention, feed-forward, multi-head, pre-norm, real-valued, permutation-invariant/equivariant, cross-modal, cross-time, continuous-time, dead-reckoning(in abstract claim), distance-weighted, root-mean-square, end-to-end, search-and-rescue, ground-truth(mod), single-modality, multi-rate, within-session, cross-session(protocol), cross-subject, cross-user, ten-day, hand-set, filter-based, camera-only, ODE-in-attention, set-level, non-positive, self-localization, error-prone, position-query, learned-fusion, $k$-nearest-neighbour, Umeyama-aligned, rigid-body, four-layer, IMU-only, co-recorded, session-invariant, over-smoothing, robustness-aware, WiFi-Net, IMU-CNN, PDR-from-start, WiFi--IMU. tab:datasets table cells ("Real (cross-session)", "Real (held-out paths)") left BYTE-IDENTICAL. DENSE SENTENCES LIGHTENED: (01_intro) the "error-prone ... which bounds accuracy together with the hardware and the site survey" trailing clause recast as a separate sentence ("Accuracy is further bounded by the sensing hardware and by the site survey that calibrates it."); the "challenge we address"/"address this challenge" repetition varied (first instance -> "the core problem this work targets"; supervisor-approved "In this paper, we address this challenge by proposing" signal phrase kept). (02_related) WIO-EKF/deep-variants multi-clause sentence split; the three-clause "Whole-modality dropout ...; ...; and SeFT and STraTS ..." semicolon chain broken into three sentences; the 40+-word "Beyond that domain shift ..." sentence split into a lead + two short sentences; the closing colon+semicolon chain unstacked. (03_problem) the long "Referencing every observation ... drops $t_q$ ... and simplifies the set to" sentence split, with the $\Delta t_i$ sign condition as its own sentence and "This drops $t_q$..." starting a new one; "asynchronous" de-duplicated in the first two sentences. (05_experiments) light touch only (reference-method bullets de-jargoned; no number touched). ABSTRACT + CONCLUSION CLAIMS UNCHANGED IN MEANING — named methods (WiFi fingerprinting reference, learned-fusion, dead-reckoning, open WiFi reference) and comparative verbs ("improves over", "remains competitive with", "improves on", "stays competitive", "degrades smoothly"/"degrading gracefully") intact; no claim added/removed/softened. Abstract = 189 words (<=200). Anti-AI self-check: zero banned vocab (grep), <=1 tricolon/paragraph, no recap-after-figure, varied openings, one idea per sentence. INTEGRITY (reading + grep; no LaTeX engine on this machine): brace/begin-end balance unchanged vs baseline (main 47/47 1/1; intro 16/16 1/1; related 24/24 2/2; problem 116/116 13/13; experiments 54/54 5/5); every `\label`/`\ref`/`\eqref`/`\cite`/`\includegraphics` key identical (ripgrep-enumerated per file); all 05_experiments protected numbers identical count; tab:datasets tabular block byte-identical (diff clean); zero `\color`; `\title` unchanged. Edited ONLY the five main/section files (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/supervisor review |
| 2026-06-25 | Abstract (paper/main.tex `\abstract{}` ONLY) | Full abstract rewrite with AI assistance to fix an external co-author's verdict ("confusing, too much information without explanation, long sentences"); director wants it understandable, polished, and catchy for abstract-screening. No number, citation, equation, label, table, figure, or the `\ifblind`/author block touched; no `\color`/`\textcolor`. WHAT CHANGED (presentation/clarity only): (1) SHORT SENTENCES — the long "We present a continuous-time set-transformer that represents every observation as one token ... without resampling or a differential-equation solver" multi-clause sentence was split into three short ones (set framing / per-token contents / single-block fusion); every sentence now carries one idea. (2) JARGON GLOSSED — "permutation-invariant self-attention block" -> plain "A single attention block fuses across sensors and time, independent of token order" (the bare term "permutation-invariant" REMOVED from the abstract); "modality- and instant-dropout" -> plain mechanism "During training we randomly hide whole sensors and single time steps, so the model learns to cope when inputs go missing or stale" (the coined compound names REMOVED); "without ... a differential-equation solver" kept but as a brief contrast ("with no resampling and no differential-equation solver"). (3) CATCHY OPENER — replaced the "Indoor localization commonly fuses..." opener with the venue broad-importance -> but-gap -> what-we-do shape: hook "Knowing where a device is inside a building underpins navigation and many location-aware services." -> the WiFi+RSSI / inertial recipe -> the async/missing/stale tension -> the resample-and-branch status quo -> "We instead present...". (4) DEVICE-AGNOSTIC framing kept; the external reviewer's pencilled-in "robot localization" first-line edit was deliberately NOT taken (grep-confirmed: no "robot" in the abstract). (5) TERMINOLOGY — model named "continuous-time transformer" (NOT "set-transformer") to match the rewritten Introduction; mechanism still described as treating observations "as an unordered set"; grep-confirmed no "set-transformer" / no "permutation-invariant" / no "modality-/instant-dropout" remain. RESULTS CLAIMS UNCHANGED IN MEANING (supervisor-approved, verbatim equivalence): (a) "It improves over an open WiFi fingerprinting reference under cross-session drift" (was "improves over an open WiFi fingerprinting reference method under cross-session drift"); (b) "stays competitive with learned-fusion and dead-reckoning references" (was "remains competitive with learned-fusion and dead-reckoning reference methods"); (c) "degrades smoothly under missing and stale inputs" (identical). Validation scope preserved: "Each encoder is validated on its benchmark, and the fused model on simulated and real data, including a cross-session collection tested on unseen days." NO number, percentage, or citation added; "(RSSI)" acronym definition kept on first use. Anti-AI self-check: zero banned vocab (grep-confirmed), tricolons = 2 across the abstract but both load-bearing and irreducible (the token's three fields; the three approved result claims) and the decorative opener tricolon was cut, one "We"-starter only (no 3-in-a-row), one idea per sentence, varied openings, no "In this paper, we propose..." opener, no method-hype adjective. WORD COUNT: 198 words (<=200 ICINCO ceiling), confirmed two ways (`wc -w` and Python `str.split()`). INTEGRITY: `\abstract{}` brace-balanced; edit confined to the abstract body; preamble / title / `\ifblind` / author block / `\keywords` / `\input` untouched; zero `\color`/`\textcolor`. Edited ONLY paper/main.tex (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep + word-count scripts. | Pending author/director review |
| 2026-06-26 | BODY register cleanup, GREEN-marked (paper/sections/01_introduction.tex, 02_related_work.tex, 03_problem_statement.tex, 06_results.tex + the `\section{Conclusion}` prose in paper/main.tex) | English-register / "oral-English" cleanup requested by senior author Yuehua, who flagged spoken expressions in the body (his red comment in the abstract block lists the examples: so->Consequently/Thus, very different->different, line up in time->align in time, free of resampling->without resampling, go missing->are missing, far smaller->significantly smaller). Director asked the assistant to APPLY the fixes and mark each change in GREEN (`\textcolor{green}{...}`) so Yuehua can see what changed; RED is his colour and was left untouched throughout. SCOPE: body only — the 6 section files + the Conclusion block of main.tex; the abstract, title, keywords, and preamble were NOT touched (those are Yuehua's, already edited). 13 GREEN fixes applied, wrapping ONLY the changed word(s)/phrase: 01_introduction (5) — "barely penetrate"->"scarcely penetrate"; "so an indoor device has to"->"Consequently, an indoor device must" (causal "so" -> formal connective, sentence split with the green span carrying the period+capitalization); "far higher"->"significantly higher"; "may drop out"->"may become unavailable"; "stays accurate"->"remains accurate". 02_related_work (1) — fixed the typo+awkward "was intuitivly presented in Time2Vec and mTAN"->"builds on Time2Vec and mTAN". 03_problem_statement (1) — "far higher"->"significantly higher". 06_results (3) — "and it does so on a far smaller budget"->"which it incurs on a significantly smaller budget" (colloquial "does so" reworded + "far smaller"->"significantly smaller"); "holds position"->"maintains position"; "stays above"->"remains above". main.tex Conclusion (3) — "stays competitive"->"remains competitive"; "far smaller"->"significantly smaller"; "much larger"->"significantly larger". 04_methodology and 05_experiments: NO extreme informal expressions found — every "so" in them is a tightly-bound mid-sentence result subordinator (standard scientific register), left intentionally per the explicit "don't robotically convert every 'so', vary the replacement, leave a few tightly-bound ones" guidance; tightly-bound "so" clauses elsewhere (intro/results/problem) also left for the same reason. Register intentionally favours formal academic English this pass at Yuehua's explicit request, kept natural rather than stiff/robotic. INTEGRITY: NO number, equation, label, `\cite`, `\ref`, table cell, figure, or caption metric changed — register/wording only; grep confirmed every flagged extreme phrase is gone from the section files (the only residual "far smaller"/"line up" match is INSIDE Yuehua's red comment in the abstract, correctly preserved). All existing `\textcolor{red}{...}` comments and `\sout{...}` preserved exactly. 13 `\textcolor{green}{` opens, all closed and brace-balanced (two span a line break in the main.tex Conclusion — "significantly\nsmaller}" and "significantly\nlarger}" — verified by reading); only GREEN added, no RED added/removed/recoloured; `xcolor` already loaded so `\textcolor{green}` renders. Edited ONLY the 6 section files + the Conclusion block of main.tex (+ PAPER_STATE.md, this file). Could NOT compile — no LaTeX engine on this machine; verified by reading + grep. | Pending author/director (Yuehua) review |
