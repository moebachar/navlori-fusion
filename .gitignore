# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/

# Data (tracked by DVC)
data/async_collection/
!/data/.gitkeep

# Third-party datasets (downloaded, not tracked)
data/iln20/
data/iln20_*/

# Experiment tracking
mlruns/
tb_logs/

# Services (binaries + runtime)
src/services/influxdb/
src/services/grafana/bin/
src/services/grafana/data/
*.log

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store

# DVC local cache
/dvc_store/

# ViT backbone feature cache
.vit_cache/

# Secrets / local service credentials
.env

# Local tool state
.jarvis/
.pytest_cache/

# arXiv code-release package (2.9 GB, becomes its own repo later)
release/

# Paper materials — tracked on branch paper-icinco-2026 only, not on main
# (paper/ is additionally its own git repo synced with Overleaf)
paper/
paper-workspace/
revision/

# Debug render outputs
_debug_pngs/

# Webots world backups + floor-orientation test renders
src/simulation/worlds/*.bak*
src/simulation/worlds/*_flip*.png
src/simulation/worlds/*_rot180.png

# Experiment outputs (16 legacy JSONs stay tracked; new outputs are local-only)
runs/
runs/**/*.pt

.claude/*
CLAUDE.md
# ICINCO paper agent
paper-workspace/zotero-export.json
paper-workspace/corpus-pdfs/
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
paper/main.pdf

# Re-include paper agent files (override earlier .claude/ ignore)
!.claude/agents/
!.claude/agents/**
!.claude/skills/paper-content/
!.claude/skills/paper-content/**
!.claude/skills/paper-format/
!.claude/skills/paper-format/**

!.claude/skills/
!.claude/skills/paper-content/
!.claude/skills/paper-format/
